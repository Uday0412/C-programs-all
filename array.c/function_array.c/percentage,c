#include<stdio.h>
int main()
{
    float average,n;
    for(int i=0;i<n;i++)
    {
        scanf("%d",&n);
    }
    int sum=0;
    sum=sum+i;
    average=sum/6;
    printf("%f",average);
    return 0;

}